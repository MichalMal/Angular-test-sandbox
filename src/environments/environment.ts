export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api', // Example API URL
  EDF_HEADER_SIZE: 256, // Replace with the actual size of the header
  EDF_SIGNAL_INFO_SIZE: 256, // Replace with the actual size of signal info
  EDF_RECORD_SIZE: 256, // Replace with the actual size of each record
  EDF_SIGNAL_BYTE_SIZE: 256, // Replace with the actual size of each signal
};
